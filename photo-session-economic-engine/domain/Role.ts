/**
 * Roles responsible for stages in the photo session workflow.
 */
export enum Role {
    PHOTOGRAPHER = 'PHOTOGRAPHER',
    RETUSHER = 'RETUSHER',
    PRINTER = 'PRINTER',
    SELLER = 'SELLER',
}
