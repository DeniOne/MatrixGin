# Модуль: ОФС – Организационно-Функциональная Схема

**Приоритет:** ВЫСОКИЙ (Пользовательский приоритет)  
**Срок:** Phase 1 (по приоритету владельца)  
**Команда:** 1 Backend + 1 Frontend

---

## 📋 ОПИСАНИЕ

Система управления организационной структурой компании с визуализацией, синхронизацией ролей и полным аудитом изменений.

### Цели проекта

✅ **Создание единой системы управления организационной структурой**  
✅ **Цифровизация процессов управления персоналом и структурой отделов**  
✅ **Обеспечение прозрачности организационной структуры**

### Основные функции

- **Регистрация и учет сотрудников** - полный профиль с должностями и компетенциями
- **Онбординг через Telegram Bot** - автоматизированная регистрация новых сотрудников
- **Гибкая система отделов** - древовидная структура с неограниченной вложенностью
- **Визуализация структуры** - интерактивная org-chart с drag & drop
- **Матрица ролей (МДР)** - Матрица должностной роли с полным описанием требований
- **Пирамида Взаимозависимости** - управление 4 вершинами: Management, Photographers, Production, Sales
- **Треугольник Взаимозависимости** - трекинг ключевых ролей: Фотографы, Дизайнеры, Продавцы
- **Корпоративный Университет** - связь с факультетами и уровнями КУ
- **История изменений** - полный аудит всех модификаций
- **Настройка подчиненности** - гибкое распределение обязанностей
- **Функции и ЦКП отделов** - управление целями и ключевыми показателями
- **Аналитические дашборды** - отчеты для руководителей
- **Экспорт документов** - PDF/Markdown/Excel

---

## 📝 СИСТЕМА РЕГИСТРАЦИИ СОТРУДНИКОВ

### Обзор процесса

Система регистрации сотрудников в MatrixGin автоматизирована через Telegram бот и обеспечивает полный цикл онбординга:

1. **Инициация** - Администратор локации отправляет Telegram ID кандидата администратору программы
2. **Приглашение** - Админ программы отправляет приглашение через бот
3. **Регистрация** - Кандидат проходит пошаговую регистрацию в боте
4. **Проверка** - HR/Админ проверяет и одобряет заявку
5. **Активация** - Создается учетная запись и профиль сотрудника

### Процесс регистрации (11 шагов)

#### Шаг 1: Фото профиля (Селфи)
**Обязательное поле**

- Кандидат отправляет своё селфи
- Можно сделать фото или загрузить из галереи
- Требования: четкое фото на нейтральном фоне
- Формат: фото (не документ)

```
Бот: "📸 Шаг 1/11: Фото профиля

Пожалуйста, отправь своё селфи.

Ты можешь:
• Сделать фото прямо сейчас 📷
• Загрузить из галереи 🖼️

Фото должно быть четким и на нейтральном фоне"
```

#### Шаг 2: ФИО
**Обязательное поле**

- Формат: Фамилия Имя Отчество
- Пример: `Иванов Иван Иванович`
- Минимум 2 слова (Фамилия + Имя)
- Отчество опционально

```
Бот: "👤 Шаг 2/11: ФИО

Введи свои Фамилию, Имя и Отчество в формате:
Иванов Иван Иванович"
```

#### Шаг 3: Дата рождения
**Обязательное поле**

- Формат: ДД.ММ.ГГГГ
- Пример: `15.03.1990`
- Валидация: возраст >= 18 лет
- Проверка корректности даты

```
Бот: "📅 Шаг 3/11: Дата рождения

Введи дату рождения в формате:
ДД.ММ.ГГГГ (например: 15.03.1990)"
```

#### Шаг 4: Адрес регистрации
**Обязательное поле**

- Полный адрес регистрации по паспорту
- Минимум 10 символов
- Пример: `г. Минск, ул. Ленина, д. 10, кв. 5`

```
Бот: "🏠 Шаг 4/11: Адрес регистрации

Введи адрес регистрации (по паспорту):
Например: г. Минск, ул. Ленина, д. 10, кв. 5"
```

#### Шаг 5: Адрес проживания
**Обязательное поле**

- Опция: Совпадает с адресом регистрации?
  - ✅ Да - адрес копируется автоматически
  - ❌ Нет - ввести другой адрес
- Если адреса разные - ввод аналогично шагу 4

```
Бот: "🏡 Шаг 5/11: Адрес проживания

Совпадает ли адрес проживания с адресом регистрации?"

Кнопки: [✅ Да, совпадает] [❌ Нет, ввести другой]
```

#### Шаг 6: Телефон
**Обязательное поле**

- Формат: международный (+375291234567)
- Валидация: 10-15 цифр
- Допустимые символы удаляются: пробелы, дефисы, скобки

```
Бот: "📱 Шаг 6/11: Номер телефона

Введи номер телефона в международном формате:
Например: +375291234567"
```

#### Шаг 7: Email
**Обязательное поле**

- Формат: стандартный email
- Валидация: regex проверка
- Пример: `ivanov@example.com`
- Конвертация в lowercase

```
Бот: "📧 Шаг 7/11: Email

Введи адрес электронной почты:
Например: ivanov@example.com"
```

#### Шаг 8: Должность
**Обязательное поле**

- Название должности
- Минимум 3 символа
- Пример: `Менеджер по продажам`

```
Бот: "💼 Шаг 8/11: Должность

Введи должность, на которую устраиваешься:
Например: Менеджер по продажам"
```

#### Шаг 9: Локация
**Обязательное поле**

- Выбор из списка активных локаций
- Inline кнопки с названиями локаций
- Отображение: Название (Город)

```
Бот: "📍 Шаг 9/11: Локация

Выбери локацию, где будешь работать:"

Кнопки: 
[Филиал на Мира (Минск)]
[Филиал на Победы (Минск)]
[Головной офис (Минск)]
```

#### Шаг 10: Скан паспорта
**Обязательное поле**

- Фото или скан разворота паспорта с фотографией
- Форматы: фото или документ
- Требование: все данные должны быть читаемы

```
Бот: "🎫 Шаг 10/11: Скан паспорта

Загрузи скан или фото разворота паспорта с фотографией.

Убедись, что все данные читаемы"
```

#### Шаг 11: Дополнительные документы
**Опциональное поле**

- Любые дополнительные документы
- Примеры: дипломы, сертификаты, рекомендации
- Можно загрузить несколько файлов
- Можно пропустить

```
Бот: "📎 Шаг 11/11: Дополнительные документы (опционально)

Если есть дополнительные документы (дипломы, сертификаты и т.д.), 
можешь загрузить их сейчас.

Если нет, нажми 'Завершить регистрацию'"

Кнопки: 
[✅ Завершить регистрацию]
[📎 Загрузить документы]
```

### Завершение регистрации

После нажатия "Завершить регистрацию":

1. **Статус изменяется**: `IN_PROGRESS` → `REVIEW`
2. **Сохраняется история**: Все шаги записываются в `registration_step_history`
3. **Уведомление кандидата**:
```
"🎉 Поздравляем!

Регистрация успешно завершена!

Твои данные отправлены на проверку HR-отделу. 
Мы свяжемся с тобой в ближайшее время.

Спасибо за терпение! 😊"
```

4. **Уведомление администраторов**: Все пользователи с ролями `ADMIN` и `HR_MANAGER` получают уведомление:
```
"📋 Новая заявка на регистрацию сотрудника

👤 Иванов Иван Иванович
📧 ivanov@example.com
📱 +375291234567
💼 Менеджер по продажам

Дата подачи: 23.11.2025 14:30"
```

### Проверка и одобрение заявки

#### Просмотр заявки (HR/Admin)

**API:** `GET /api/registration/requests/:id`

Отображаемая информация:
- Все введенные данные
- Загруженные фото и документы
- История прохождения шагов
- Дата и время каждого шага
- Кто пригласил кандидата

#### Одобрение заявки

**API:** `POST /api/registration/requests/:id/approve`

Процесс одобрения:
1. Создается учетная запись пользователя (`users` table)
2. Создается профиль сотрудника (`employees` table)
3. Генерируется временный пароль
4. Отправляется email с данными для входа
5. Отправляется уведомление в Telegram:

```
"🎉 Поздравляем!

Твоя регистрация одобрена!

Добро пожаловать в команду MatrixGin! 🚀

Теперь ты можешь использовать все функции системы. 
Временный пароль для входа отправлен на email: ivanov@example.com"
```

#### Отклонение заявки

**API:** `POST /api/registration/requests/:id/reject`

Требуется:
- Причина отклонения (обязательное поле)

Уведомление кандидату:
```
"❌ К сожалению, твоя заявка на регистрацию была отклонена.

Причина: [Указанная причина]

Если у тебя есть вопросы, пожалуйста, свяжись с HR-отделом."
```

### База данных

#### Таблица: `employee_registration_requests`

```sql
CREATE TABLE employee_registration_requests (
    id UUID PRIMARY KEY,
    telegram_id VARCHAR(50) UNIQUE NOT NULL,
    telegram_username VARCHAR(100),
    
    -- Status
    status registration_status DEFAULT 'PENDING',
    current_step registration_step DEFAULT 'PHOTO',
    
    -- Personal data
    photo_url TEXT,
    first_name VARCHAR(100),
    last_name VARCHAR(100),
    middle_name VARCHAR(100),
    birth_date DATE,
    
    -- Addresses
    registration_address TEXT,
    residential_address TEXT,
    addresses_match BOOLEAN DEFAULT FALSE,
    
    -- Contacts
    phone VARCHAR(20),
    email VARCHAR(255),
    
    -- Employment
    position VARCHAR(255),
    location_id UUID REFERENCES locations(id),
    department_id UUID REFERENCES departments(id),
    
    -- Documents
    passport_scan_url TEXT,
    additional_documents JSONB DEFAULT '[]',
    
    -- Tracking
    invited_by UUID REFERENCES users(id),
    invitation_sent_at TIMESTAMPTZ,
    reviewed_by UUID REFERENCES users(id),
    reviewed_at TIMESTAMPTZ,
    rejection_reason TEXT,
    
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    completed_at TIMESTAMPTZ
);
```

#### Таблица: `registration_step_history`

```sql
CREATE TABLE registration_step_history (
    id UUID PRIMARY KEY,
    registration_id UUID REFERENCES employee_registration_requests(id),
    step registration_step NOT NULL,
    data JSONB,
    completed_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### Статусы регистрации

```typescript
enum RegistrationStatus {
    PENDING = 'PENDING',           // Приглашение отправлено
    IN_PROGRESS = 'IN_PROGRESS',   // Процесс регистрации
    DOCUMENTS_PENDING = 'DOCUMENTS_PENDING', // Ожидание документов
    REVIEW = 'REVIEW',             // На проверке у HR
    APPROVED = 'APPROVED',         // Одобрено
    REJECTED = 'REJECTED'          // Отклонено
}
```

### API Endpoints

#### POST `/api/registration/invite`
Отправить приглашение на регистрацию

**Требования:** Admin или HR Manager

**Request:**
```json
{
  "telegramId": "123456789",
  "departmentId": "dept-uuid",
  "locationId": "location-uuid"
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "message": "Registration invitation sent successfully"
  }
}
```

#### GET `/api/registration/requests`
Получить список заявок

**Query Parameters:**
```
?status=REVIEW
&page=1
&limit=20
```

**Response:**
```json
{
  "success": true,
  "data": [
    {
      "id": "reg-uuid",
      "telegram_id": "123456789",
      "first_name": "Иван",
      "last_name": "Иванов",
      "middle_name": "Иванович",
      "email": "ivanov@example.com",
      "phone": "+375291234567",
      "position": "Менеджер по продажам",
      "status": "REVIEW",
      "current_step": "COMPLETED",
      "department_name": "Отдел продаж",
      "location_name": "Филиал на Мира",
      "created_at": "2025-11-23T10:00:00Z",
      "completed_at": "2025-11-23T14:30:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 5,
    "totalPages": 1
  }
}
```

#### GET `/api/registration/requests/:id`
Получить детали заявки

**Response:**
```json
{
  "success": true,
  "data": {
    "id": "reg-uuid",
    "telegram_id": "123456789",
    "first_name": "Иван",
    "last_name": "Иванов",
    "photo_url": "telegram://photo/ABC123",
    "birth_date": "1990-03-15",
    "registration_address": "г. Минск, ул. Ленина, д. 10, кв. 5",
    "residential_address": "г. Минск, ул. Ленина, д. 10, кв. 5",
    "addresses_match": true,
    "phone": "+375291234567",
    "email": "ivanov@example.com",
    "position": "Менеджер по продажам",
    "passport_scan_url": "telegram://file/DEF456",
    "additional_documents": [
      {
        "name": "diploma.pdf",
        "url": "telegram://file/GHI789",
        "type": "application/pdf",
        "uploaded_at": "2025-11-23T14:20:00Z"
      }
    ],
    "status": "REVIEW",
    "step_history": [
      {
        "step": "PHOTO",
        "data": {"photo_url": "telegram://photo/ABC123"},
        "completed_at": "2025-11-23T10:05:00Z"
      },
      {
        "step": "FULL_NAME",
        "data": {"first_name": "Иван", "last_name": "Иванов"},
        "completed_at": "2025-11-23T10:07:00Z"
      }
      // ... остальные шаги
    ]
  }
}
```

#### POST `/api/registration/requests/:id/approve`
Одобрить заявку

**Response:**
```json
{
  "success": true,
  "data": {
    "message": "Registration approved successfully"
  }
}
```

#### POST `/api/registration/requests/:id/reject`
Отклонить заявку

**Request:**
```json
{
  "reason": "Неполные документы"
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "message": "Registration rejected successfully"
  }
}
```

#### GET `/api/registration/stats`
Статистика по регистрациям

**Response:**
```json
{
  "success": true,
  "data": {
    "by_status": [
      {"status": "PENDING", "count": 3},
      {"status": "IN_PROGRESS", "count": 5},
      {"status": "REVIEW", "count": 8},
      {"status": "APPROVED", "count": 45},
      {"status": "REJECTED", "count": 2}
    ],
    "recent_registrations": [
      {"date": "2025-11-23", "count": 5},
      {"date": "2025-11-22", "count": 3},
      {"date": "2025-11-21", "count": 7}
    ]
  }
}
```

### Инструкция для администратора

#### 1. Получение Telegram ID кандидата

**Способ 1:** Попросить кандидата написать боту `@MatrixGin_bot` команду `/start` - бот покажет его Telegram ID

**Способ 2:** Использовать бота `@userinfobot` - переслать любое сообщение кандидата этому боту

#### 2. Отправка приглашения

```bash
# Через API
curl -X POST http://localhost:3000/api/registration/invite \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "telegramId": "123456789",
    "departmentId": "dept-uuid-123",
    "locationId": "location-uuid-456"
  }'
```

Или через Admin Panel (Frontend) - форма приглашения сотрудника

#### 3. Мониторинг регистраций

- Проверка списка заявок: `GET /api/registration/requests?status=REVIEW`
- При поступлении новой заявки - автоматическое уведомление в Telegram
- Просмотр деталей: `GET /api/registration/requests/:id`

#### 4. Проверка документов

Проверить:
- ✅ Фото профиля четкое
- ✅ ФИО корректно
- ✅ Дата рождения (возраст >= 18)
- ✅ Адреса заполнены полностью
- ✅ Телефон в правильном формате
- ✅ Email валидный
- ✅ Должность соответствует вакансии
- ✅ Локация выбрана верно
- ✅ Скан паспорта читаем

#### 5. Одобрение/Отклонение

**Одобрить:**
```bash
curl -X POST http://localhost:3000/api/registration/requests/:id/approve \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

**Отклонить:**
```bash
curl -X POST http://localhost:3000/api/registration/requests/:id/reject \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"reason": "Неполные документы. Пожалуйста, загрузите четкий скан паспорта."}'
```

### Валидация данных

#### Email
```regex
^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}$
```

#### Телефон
```regex
^\+?[0-9]{10,15}$
```

#### Дата рождения
- Формат: DD.MM.YYYY
- Возраст: >= 18 лет
- Валидная дата (не будущая)

#### Адрес
- Минимум 10 символов
- Обязательное поле

### Безопасность

- 🔒 Все API endpoints требуют JWT аутентификацию
- 🔒 Только роли `ADMIN` и `HR_MANAGER` имеют доступ
- 🔒 Персональные данные обрабатываются согласно 152-ФЗ
- 🔒 Все действия логируются в `audit_logs`
- 🔒 Файлы хранятся с шифрованием (в продакшене)
- 🔒 Временный пароль генерируется криптографически безопасным методом

### Метрики и мониторинг

- 📊 Количество отправленных приглашений
- 📊 Процент завершенных регистраций
- 📊 Среднее время прохождения регистрации
- 📊 Процент одобренных заявок
- 📊 Наиболее частые причины отклонения
- 📊 Динамика регистраций по дням/неделям

---

## 🔷 ПИРАМИДА И ТРЕУГОЛЬНИК ВЗАИМОЗАВИСИМОСТИ

### Концепция Пирамиды Взаимозависимости

В масштабах всей компании оперируем понятием **Пирамиды Взаимозависимости**, в основе которой лежат три операционные вершины (Фотографы, Производство, Продажи), а **четвертая, равнозначная и критически важная вершина – УПРАВЛЕНИЕ**.

#### 4 Вершины Пирамиды:

1. **MANAGEMENT (Управление)**
   - Функция: Стратегическое видение, синхронизация департаментов, выделение ресурсов
   - Устранение системных потерь, разработка Золотых Стандартов
   - Контроль KPI, поддержание корпоративной культуры
   - Взаимозависимость: Создает условия для эффективной работы всех вершин

2. **PHOTOGRAPHERS (Фотографы)**
   - Функция: Создание первичной ценности - качественный фотоматериал и "горячий лид"
   - KPI: Количество и качество исходников, конверсия в продажи
   - Взаимозависимость: Без качественных исходников нет продукта для Production и Sales

3. **PRODUCTION (Производство)**
   - Функция: Превращение ценности в готовый продукт (ретушь, дизайн, печать)
   - KPI: Качество обработки, скорость, процент брака
   - Взаимозависимость: Качество влияет на продажи и репутацию

4. **SALES (Продажи)**
   - Функция: Реализация ценности и получение прибыли
   - KPI: Выручка, средний чек, конверсия
   - Взаимозависимость: Определяет общий доход всех сотрудников

### Концепция Треугольника Взаимозависимости

В основе операционной деятельности - равнобедренный **Треугольник из 3 ключевых ролей**:
- Фотографы
- Ретушеры/Дизайнеры  
- Продавцы

Эти роли **абсолютно равнозначны** и **критически взаимозависимы**. Успех каждого напрямую влияет на результат других.

**Эффективность измеряется:**
- Скорость потока создания ценности
- Качество на каждом этапе
- Минимизация потерь (ожидания, брака, лишних действий)

### API Endpoints - Пирамида и Треугольник

#### GET `/api/ofs/pyramid`
Получить роли Пирамиды Взаимозависимости

```json
{
  "success": true,
  "data": [
    {
      "id": "pyramid-role-uuid",
      "role_type": "management",
      "role_name": "Генеральный Директор",
      "kpi_metrics": {...}
    }
  ]
}
```

#### POST `/api/ofs/pyramid`
Создать роль в Пирамиде

#### GET `/api/ofs/triangle`
Получить назначения Треугольника (`?employee_id=uuid`)

#### POST `/api/ofs/triangle/assign`
Назначить сотрудника на роль в Треугольнике

#### GET `/api/ofs/triangle/stats`
Статистика по Треугольнику

---

## 🎓 КОРПОРАТИВНЫЙ УНИВЕРСИТЕТ

### Интеграция с КУ

Модуль ОФС полностью интегрирован с Корпоративным Университетом (КУ):

**6 Факультетов:**
1. Факультет Фотомастерства
2. Факультет Продаж и Сервиса
3. Факультет Корпоративной Культуры
4. Факультет Личностного Развития
5. Факультет Технологий и Оборудования
6. Факультет Менеджмента

**Уровни квалификации:** Фотон, Топчик, Снайпер, Боец, Мастер

**Расширения БД:**
```sql
-- Departments
ALTER TABLE departments ADD COLUMN faculty_link VARCHAR(100);

-- Employees  
ALTER TABLE employees ADD COLUMN corporate_university_level VARCHAR(50);
ALTER TABLE employees ADD COLUMN faculty VARCHAR(100);

-- Roles
ALTER TABLE role_competency_matrix ADD COLUMN corporate_university_courses TEXT[];
```

---

## 🗄️ DATABASE SCHEMA

### Расширение существующих таблиц

```sql
-- Departments уже существует, добавляем поля для ОФС
ALTER TABLE departments ADD COLUMN IF NOT EXISTS functions TEXT[];
ALTER TABLE departments ADD COLUMN IF NOT EXISTS kpis JSONB;
ALTER TABLE departments ADD COLUMN IF NOT EXISTS annual_goals TEXT;
ALTER TABLE departments ADD COLUMN IF NOT EXISTS code VARCHAR(50) UNIQUE;
ALTER TABLE departments ADD COLUMN IF NOT EXISTS budget_annual DECIMAL(15,2);
ALTER TABLE departments ADD COLUMN IF NOT EXISTS is_active BOOLEAN DEFAULT TRUE;
ALTER TABLE departments ADD COLUMN IF NOT EXISTS level INTEGER DEFAULT 0;
ALTER TABLE departments ADD COLUMN IF NOT EXISTS path LTREE;

-- Employees уже существует, добавляем поля для компетенций
ALTER TABLE employees ADD COLUMN IF NOT EXISTS competencies JSONB;
ALTER TABLE employees ADD COLUMN IF NOT EXISTS certifications JSONB;
ALTER TABLE employees ADD COLUMN IF NOT EXISTS skills TEXT[];

-- Создать индексы для быстрого поиска
CREATE INDEX IF NOT EXISTS idx_departments_path ON departments USING GIST(path);
CREATE INDEX IF NOT EXISTS idx_departments_level ON departments(level);
CREATE INDEX IF NOT EXISTS idx_employees_skills ON employees USING GIN(skills);
```

### Новые таблицы

```sql
-- Матрица ролей и компетенций
CREATE TABLE role_competency_matrix (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    role_name VARCHAR(255) NOT NULL,
    department_id UUID REFERENCES departments(id) ON DELETE CASCADE,
    
    -- Требуемые компетенции
    required_competencies JSONB NOT NULL,
    -- Пример: {"leadership": 5, "technical": 7, "communication": 6}
    
    -- Обязанности
    responsibilities TEXT[],
    
    -- Права доступа
    permissions JSONB,
    
    -- Диапазон зарплаты
    salary_min DECIMAL(15,2),
    salary_max DECIMAL(15,2),
    
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    
    UNIQUE(role_name, department_id)
);

-- Назначение ролей сотрудникам
CREATE TABLE employee_roles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    employee_id UUID REFERENCES employees(id) ON DELETE CASCADE,
    role_matrix_id UUID REFERENCES role_competency_matrix(id) ON DELETE CASCADE,
    
    -- Дата назначения
    assigned_at TIMESTAMPTZ DEFAULT NOW(),
    effective_from DATE NOT NULL,
    effective_to DATE,
    
    -- Кем назначено
    assigned_by UUID REFERENCES users(id),
    
    -- Статус
    is_active BOOLEAN DEFAULT TRUE,
    
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- История изменений организационной структуры
CREATE TABLE org_structure_history (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    entity_type VARCHAR(50) NOT NULL, -- 'department', 'employee', 'role'
    entity_id UUID NOT NULL,
    
    action VARCHAR(50) NOT NULL, -- 'created', 'updated', 'deleted', 'moved', 'assigned'
    
    changed_by UUID REFERENCES users(id),
    
    -- Данные до изменения
    old_data JSONB,
    
    -- Данные после изменения
    new_data JSONB,
    
    -- Причина изменения
    reason TEXT,
    
    -- IP и метаданные
    ip_address INET,
    user_agent TEXT,
    
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Подчиненность (дополнительно к manager_id в employees)
CREATE TABLE reporting_relationships (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    subordinate_id UUID REFERENCES employees(id) ON DELETE CASCADE,
    supervisor_id UUID REFERENCES employees(id) ON DELETE CASCADE,
    
    relationship_type VARCHAR(50) DEFAULT 'direct', -- 'direct', 'functional', 'dotted_line'
    
    effective_from DATE NOT NULL,
    effective_to DATE,
    
    is_active BOOLEAN DEFAULT TRUE,
    
    created_at TIMESTAMPTZ DEFAULT NOW(),
    
    CONSTRAINT no_self_reporting CHECK (subordinate_id != supervisor_id)
);

-- Индексы
CREATE INDEX idx_role_matrix_dept ON role_competency_matrix(department_id);
CREATE INDEX idx_employee_roles_employee ON employee_roles(employee_id);
CREATE INDEX idx_employee_roles_active ON employee_roles(is_active) WHERE is_active = TRUE;
CREATE INDEX idx_org_history_entity ON org_structure_history(entity_type, entity_id);
CREATE INDEX idx_org_history_date ON org_structure_history(created_at DESC);
CREATE INDEX idx_reporting_active ON reporting_relationships(is_active) WHERE is_active = TRUE;

-- Comments
COMMENT ON TABLE role_competency_matrix IS 'Матрица должностей с требуемыми компетенциями';
COMMENT ON TABLE employee_roles IS 'Назначение ролей сотрудникам';
COMMENT ON TABLE org_structure_history IS 'Полная история изменений организационной структуры';
COMMENT ON TABLE reporting_relationships IS 'Отношения подчиненности (множественные линии отчетности)';
```

---

## 🔌 API ENDPOINTS

### Departments Management

#### GET `/api/ofs/departments`
Список всех департаментов с иерархией

**Query Parameters:**
```
?include_inactive=false
&format=tree|flat
&with_stats=true
```

**Response (200):**
```json
{
  "success": true,
  "data": [
    {
      "id": "dept-uuid-1",
      "name": "Коммерческий департамент",
      "code": "COMM",
      "description": "Продажи и работа с клиентами",
      "level": 0,
      "path": "dept-uuid-1",
      "parent_id": null,
      "head": {
        "id": "emp-uuid-1",
        "name": "Иванов Иван Иванович",
        "position": "Коммерческий директор"
      },
      "functions": [
        "Продажи",
        "Маркетинг",
        "Клиентский сервис"
      ],
      "kpis": {
        "revenue_target": 50000000,
        "client_satisfaction": 95
      },
      "employees_count": 25,
      "budget_annual": 5000000,
      "is_active": true,
      "children": [
        {
          "id": "dept-uuid-2",
          "name": "Отдел продаж",
          "level": 1,
          "employees_count": 15
        }
      ]
    }
  ]
}
```

#### POST `/api/ofs/departments`
Создать новый департамент

**Request:**
```json
{
  "name": "Отдел инноваций",
  "code": "INNOV",
  "description": "Разработка новых продуктов",
  "parent_id": "dept-uuid-1",
  "head_id": "emp-uuid-5",
  "functions": ["R&D", "Прототипирование"],
  "kpis": {
    "new_products_per_year": 5,
    "innovation_index": 80
  },
  "budget_annual": 2000000
}
```

#### PUT `/api/ofs/departments/{id}`
Обновить департамент

#### DELETE `/api/ofs/departments/{id}`
Удалить/деактивировать департамент (soft delete)

#### POST `/api/ofs/departments/{id}/move`
Переместить департамент в другое место иерархии

**Request:**
```json
{
  "new_parent_id": "dept-uuid-3",
  "reason": "Реорганизация структуры"
}
```

---

### Role Matrix Management

#### GET `/api/ofs/role-matrix`
Матрица ролей по департаментам

**Response (200):**
```json
{
  "success": true,
  "data": [
    {
      "id": "role-matrix-1",
      "role_name": "Менеджер по продажам",
      "department": {
        "id": "dept-uuid-2",
        "name": "Отдел продаж"
      },
      "required_competencies": {
        "sales": 7,
        "communication": 8,
        "crm_systems": 6
      },
      "responsibilities": [
        "Продажи услуг клиентам",
        "Ведение базы клиентов в CRM",
        "Достижение плана продаж"
      ],
      "salary_range": {
        "min": 50000,
        "max": 120000
      },
      "current_employees": 8
    }
  ]
}
```

#### POST `/api/ofs/role-matrix`
Создать новую роль в матрице

#### PUT `/api/ofs/role-matrix/{id}`
Обновить роль

#### POST `/api/ofs/role-matrix/{roleId}/assign`
Назначить роль сотруднику

**Request:**
```json
{
  "employee_id": "emp-uuid-10",
  "effective_from": "2025-12-01",
  "reason": "Повышение"
}
```

---

### Employee Management

#### GET `/api/ofs/employees`
Список сотрудников с расширенной информацией

**Query Parameters:**
```
?department_id=dept-uuid-1
&role=Менеджер
&status=active
&has_competencies=leadership,technical
&page=1
&limit=20
```

**Response (200):**
```json
{
  "success": true,
  "data": [
    {
      "id": "emp-uuid-1",
      "user": {
        "id": "user-uuid-1",
        "email": "ivanov@example.com",
        "first_name": "Иван",
        "last_name": "Иванов"
      },
      "department": {
        "id": "dept-uuid-1",
        "name": "Коммерческий департамент",
        "code": "COMM"
      },
      "position": "Коммерческий директор",
      "roles": [
        {
          "role_name": "Руководитель департамента",
          "assigned_at": "2023-01-15"
        }
      ],
      "competencies": {
        "leadership": 9,
        "sales": 8,
        "strategic_planning": 7
      },
      "certifications": [
        {
          "name": "MBA",
          "issued_by": "MSU Business School",
          "date": "2020-06-15"
        }
      ],
      "skills": ["CRM", "Sales Management", "Team Building"],
      "direct_reports_count": 5,
      "manager": {
        "id": "emp-uuid-0",
        "name": "CEO"
      }
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 125,
    "totalPages": 7
  }
}
```

#### PUT `/api/ofs/employees/{id}/competencies`
Обновить компетенции сотрудника

**Request:**
```json
{
  "competencies": {
    "leadership": 9,
    "technical": 8,
    "communication": 7
  },
  "certifications": [
    {
      "name": "PMP",
      "issued_by": "PMI",
      "date": "2024-05-10",
      "expires": "2027-05-10"
    }
  ],
  "skills": ["Project Management", "Agile", "Scrum"]
}
```

#### POST `/api/ofs/employees/{id}/transfer`
Перевести сотрудника в другой департамент

**Request:**
```json
{
  "new_department_id": "dept-uuid-5",
  "new_position": "Старший менеджер",
  "effective_date": "2025-12-01",
  "reason": "Карьерный рост"
}
```

---

### Reporting Relationships

#### GET `/api/ofs/reporting/{employeeId}`
Получить все линии отчетности сотрудника

**Response (200):**
```json
{
  "success": true,
  "data": {
    "employee": {
      "id": "emp-uuid-1",
      "name": "Иванов Иван"
    },
    "reports_to": [
      {
        "id": "rel-1",
        "supervisor": {
          "id": "emp-uuid-0",
          "name": "Генеральный директор"
        },
        "relationship_type": "direct",
        "is_active": true
      },
      {
        "id": "rel-2",
        "supervisor": {
          "id": "emp-uuid-10",
          "name": "Технический директор"
        },
        "relationship_type": "functional",
        "is_active": true
      }
    ],
    "supervises": [
      {
        "id": "emp-uuid-15",
        "name": "Петров Петр",
        "relationship_type": "direct"
      },
      {
        "id": "emp-uuid-16",
        "name": "Сидоров Сидор",
        "relationship_type": "direct"
      }
    ]
  }
}
```

#### POST `/api/ofs/reporting`
Создать новую линию отчетности

**Request:**
```json
{
  "subordinate_id": "emp-uuid-20",
  "supervisor_id": "emp-uuid-5",
  "relationship_type": "functional",
  "effective_from": "2025-12-01",
  "reason": "Матричная структура проекта"
}
```

---

### Organizational Chart

#### GET `/api/ofs/org-chart`
Данные для визуализации org-chart

**Query Parameters:**
```
?department_id=dept-uuid-1  // корневой департамент
&depth=3  // глубина иерархии
&include_photos=true
```

**Response (200):**
```json
{
  "success": true,
  "data": {
    "id": "dept-uuid-1",
    "name": "Photomatrix",
    "type": "company",
    "head": {
      "id": "emp-uuid-ceo",
      "name": "CEO",
      "photo": "https://storage.../ceo.jpg",
      "position": "Генеральный директор"
    },
    "children": [
      {
        "id": "dept-uuid-2",
        "name": "Коммерческий департамент",
        "type": "department",
        "head": {
          "id": "emp-uuid-1",
          "name": "Коммерческий директор"
        },
        "employees_count": 25,
        "children": [...]
      }
    ]
  }
}
```

---

### History & Audit

#### GET `/api/ofs/history`
История изменений организационной структуры

**Query Parameters:**
```
?entity_type=department|employee|role
&entity_id=uuid-123
&action=created|updated|deleted|moved
&date_from=2025-01-01
&date_to=2025-12-31
&changed_by=user-uuid
&page=1
&limit=50
```

**Response (200):**
```json
{
  "success": true,
  "data": [
    {
      "id": "history-1",
      "entity_type": "employee",
      "entity_id": "emp-uuid-5",
      "action": "moved",
      "changed_by": {
        "id": "user-uuid-hr",
        "name": "HR Manager"
      },
      "old_data": {
        "department_id": "dept-uuid-1",
        "position": "Менеджер"
      },
      "new_data": {
        "department_id": "dept-uuid-3",
        "position": "Старший менеджер"
      },
      "reason": "Повышение по результатам аттестации",
      "created_at": "2025-11-20T14:30:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 50,
    "total": 345
  }
}
```

---

### Reports & Export

#### GET `/api/ofs/reports/structure`
Отчет по организационной структуре

**Response (200):**
```json
{
  "success": true,
  "data": {
    "total_departments": 15,
    "total_employees": 125,
    "average_department_size": 8.3,
    "hierarchy_depth": 4,
    "departments_by_level": {
      "0": 1,
      "1": 5,
      "2": 7,
      "3": 2
    },
    "headcount_by_department": [
      {
        "department": "Коммерческий",
        "count": 25
      }
    ]
  }
}
```

#### POST `/api/ofs/export/pdf`
Экспорт org-chart в PDF

**Request:**
```json
{
  "department_id": "dept-uuid-1",
  "include_photos": true,
  "include_contacts": false,
  "format": "landscape"
}
```

**Response (200):**
```json
{
  "success": true,
  "data": {
    "download_url": "https://storage.../org-chart-2025-11-22.pdf",
    "expires_at": "2025-11-23T10:00:00Z"
  }
}
```

#### POST `/api/ofs/export/markdown`
Экспорт структуры в Markdown

#### POST `/api/ofs/export/excel`
Экспорт в Excel (сотрудники + департаменты)

---

## 🛠️ ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Backend
- **Nest.js** ✅ - основной фреймворк
- **Prisma ORM** ✅ - работа с БД
- **PostgreSQL ltree** - иерархические запросы
- **pdfmake** или **puppeteer** - генерация PDF
- **xlsx** - генерация Excel
- **@nestjs/cache-manager** - кэширование org-chart

### Frontend
- **React 18** ✅
- **Redux Toolkit** ✅
- **React Flow** или **@antv/g6** - визуализация org-chart
- **React DnD** - drag & drop для перестройки
- **Recharts** - аналитические графики
- **React Hook Form** + **Zod** - формы

---

## 📊 МЕТРИКИ УСПЕХА

- [ ] 100% департаментов отображаются корректно
- [ ] Org-chart загружается <2 секунд
- [ ] История изменений полная (100% coverage)
- [ ] Экспорт PDF/Excel работает
- [ ] Drag & drop перестройка работает
- [ ] RBAC права корректны (только HR/Admin могут редактировать)
- [ ] Покрытие тестами >80%

---

## 📝 ЗАВИСИМОСТИ

### От других модулей
- `02-Authentication-Authorization` ✅ - RBAC права
- `08-Employee-Management` ✅ - базовые данные сотрудников
- `13-Legal-Compliance` ✅ - аудит изменений

### Используется модулями
- `20-Task-Management` - назначение задач по структуре
- `12-KPI-Analytics` - KPI департаментов
- `10-HR-Analytics-Matrix360` - HR аналитика

---

## 🔒 БЕЗОПАСНОСТЬ

- **RBAC:** Только HR Manager, Admin могут изменять структуру
- **Audit Log:** Все изменения логируются в `org_structure_history`
- **Soft Delete:** Департаменты и сотрудники не удаляются физически
- **152-ФЗ Compliance:** Персональные данные обрабатываются согласно закону
