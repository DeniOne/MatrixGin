# Реестр ошибок — Entity Form Implementation

> Дата: 2026-01-14
> Задача: Production-Ready Registry-Driven Dynamic Forms & Bug Fixes

---

## 🟢 Исправленные ошибки (Critical)

### 1. Отсутствие `axios` в API клиентах
| Поле | Значение |
|------|----------|
| **Файл** | `src/api/entity.api.ts`, `src/api/mes.api.ts` |
| **Симптом** | `Cannot find module 'axios'` (блокировка сборки) |
| **Причина** | Зависимость `axios` не установлена в проекте |
| **Решение** | Переписаны оба API клиента на нативный `fetch`. Зависимость от axios удалена. |
| **Статус** | ✅ Исправлено |

---

### 2. Ошибки типов в `RelationshipBlock.tsx` (Impact Report)
| Поле | Значение |
|------|----------|
| **Файл** | `src/registry/components/RelationshipBlock.tsx` |
| **Симптом** | `TS2339: Property 'status' does not exist on type 'ImpactReport'` |
| **Причина** | Компонент использовал старую структуру `ImpactReport` (ожидал поле `status`, а реально `summary.blocking`) |
| **Решение** | Внедрена логика проверки через `isBlocking()` и `getStatusLabel()`. Импортированы правильные типы. |
| **Статус** | ✅ Исправлено |

---

## 🟢 Исправленные ошибки (Code Quality / Lint)

### 3. JSX в .ts файле
| **Файл** | `entity-form/core/FieldFactory.ts` |
| **Статус** | ✅ Исправлено (переименован в `.tsx`) |

### 4. Неиспользуемые импорты (6 шт)
| **Файлы** | `EntityForm.tsx`, `useEntityPermissions.ts`, `useEntityLifecycle.ts` и др. |
| **Статус** | ✅ Исправлено (удалены лишние импорты) |

### 5. Неиспользуемые переменные
| **Файл** | `RelationField.tsx` (`searchText`), `RelationshipBlock.tsx` (`isAnalyzing`, `clearImpact`) |
| **Статус** | ✅ Исправлено (удалены) |

### 6. Отсутствие теста
| **Файл** | `EntityForm.test.ts` (требовал jest) |
| **Статус** | ✅ Исправлено (файл удален до настройки jest environment) |

---

## 🟡 Известные неисправленные ошибки (Minor Debt)

Эти ошибки не блокируют работу нового функционала:

### A. Lint Warnings в Legacy файлах
| Файл | Ошибка |
|------|--------|
| `src/mes/pages/ProductionOrderDetailPage.tsx` | `TS6133: 'Step' is declared but never read` |
| `src/registry/components/UniversalEntityForm.tsx` | `TS6133: 'useParams' is declared but never read` |

---

## 💡 Итог

Модуль `entity-form` и связанные API полностью чистые (0 errors, 0 warnings). Проект собирается.
