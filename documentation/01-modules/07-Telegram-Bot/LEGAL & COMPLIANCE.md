LEGAL & COMPLIANCE

Documents · Signatures · Audit · Evidence-first

1. Назначение модуля

Legal & Compliance — это контур доказуемости.
Он отвечает не за “правильность”, а за “возможность доказать правильность”.

Модуль обеспечивает:

юридически значимое хранение документов

фиксацию согласий и подписей

неизменяемый аудит действий

соответствие трудовому, корпоративному и data-законодательству

2. Архитектурный принцип (жёсткий)
🔒 Compliance ≠ UX

Compliance не упрощается ради удобства

UX не нарушает юридическую целостность

3. Типы юридических документов
LegalDocumentType =
  | EMPLOYMENT_CONTRACT
  | NDA
  | DATA_PROCESSING_CONSENT
  | POLICY_ACKNOWLEDGEMENT
  | INTERNAL_REGULATION
  | SHIFT_REGULATION


Каждый документ:

имеет юридическую силу

связан с версией

применяется во времени

4. Модель юридического документа
LegalDocument {
  id: UUID
  type: LegalDocumentType

  title: string
  version: string
  status: DRAFT | ACTIVE | ARCHIVED

  file_url: string
  checksum: string

  valid_from: datetime
  valid_to?: datetime

  created_by: UUID
  approved_by: UUID

  created_at: datetime
}

5. Подписи и согласия (SIGNATURES)
5.1. Принцип подписи

Подпись — это событие, а не флаг.

5.2. Модель подписи
LegalSignature {
  id: UUID
  document_id: UUID
  employee_id: UUID

  signature_type: 
    | ELECTRONIC
    | CONFIRMATION
    | CHECKBOX_CONSENT

  signed_at: datetime

  metadata: {
    ip_address?: string
    user_agent?: string
    channel: WEB | TELEGRAM
  }
}

5.3. UX-правила подписей

документ всегда:

read-only

с версией

подпись:

отдельное действие

с явным подтверждением

нельзя:

подписать «пакетом»

подписать задним числом

подписать за другого

6. Связь документов с ролями и сменами
6.1. Применимость

Документ может быть:

обязательным для роли

обязательным перед сменой

обязательным один раз

DocumentRequirement {
  document_id: UUID
  role_code?: string
  shift_type?: string
  mandatory: boolean
}

6.2. Блокировка логики (важно)

❌ нельзя открыть смену, если:

обязательный документ не подписан

❌ нельзя назначить роль без согласия

✅ система показывает причину, не просто блокирует

7. Аудит (AUDIT LOG)
7.1. Принцип

Аудит — append-only.
Ничего не удаляется. Никогда.

7.2. Модель AuditEvent
AuditEvent {
  id: UUID

  actor_type: USER | SYSTEM
  actor_id?: UUID

  action: string
  entity_type: string
  entity_id: UUID

  timestamp: datetime

  metadata: JSON
}

7.3. Что подлежит аудиту (обязательно)

вход в систему

смена ролей

подписание документов

открытие / закрытие смен

изменение задач

любые юридически значимые действия

AI:

никогда не actor

максимум — SYSTEM с явной пометкой

8. Связь с MG (AI)
8.1. Что MG может

читать документы

напоминать о необходимости подписания

ссылаться на пункт регламента

8.2. Что MG запрещено

трактовать юридически

требовать подписи

блокировать действия

инициировать подпись

MG говорит:

«Для продолжения смены требуется согласие с документом X»

9. UX: Legal в ЛК сотрудника
9.1. Раздел «Юридические документы»

список активных документов

статус:

подписан

требуется подпись

не применим

доступ к истории подписей

10. UX: Legal для руководителя / HR

список подписей по ролям

отчёты:

кто не подписал

какие документы истекают

выгрузка для аудита

11. Data Protection (152-ФЗ / GDPR ready)
11.1. Принципы

минимизация данных

фиксированная цель

ограниченный доступ

право на экспорт

11.2. Экспорт данных сотрудника
GET /legal/export/:employee_id


Экспорт включает:

документы

подписи

аудит (по субъекту)

12. Security & Governance
Принцип	Статус
Append-only audit	✅
No retroactive changes	✅
Human-in-the-loop	✅
AI read-only	✅
Secure Core	✅
13. Каноническая схема
DOCUMENT (versioned)
   ↓
SIGNATURE (event)
   ↓
AUDIT LOG (immutable)
   ↓
COMPLIANCE EVIDENCE

14. Статус спецификации

Версия: v1.0

Статус: CANONICAL / NON-OPTIONAL

Без этого модуля система не считается production-ready