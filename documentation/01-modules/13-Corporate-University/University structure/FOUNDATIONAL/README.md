# 🛑 ВНИМАНИЕ: Управление Контуром Допуска (Module 13)

### ⚠️ ГЛАВНОЕ ПРАВИЛО (КАНОН)
**Никогда не редактируйте материалы ФУНДАМЕНТАЛЬНОГО УРОВНЯ через административную панель (UI)!**

Этот контур является юридическим и операционным порогом допуска. Любое изменение через фронтенд нарушит целостность аудита и хеш-сумму, зафиксированную в системе.

---

### 🛠️ Как вносить изменения
Все изменения должны проходить через "Code as Law" процесс:

1.  **Правка файлов:** Измените нужный `.md` файл в этой директории (`documentation/01-modules/13-Corporate-University/University structure/FOUNDATIONAL/`).
2.  **Git Commit:** Зафиксируйте изменения в репозитории.
3.  **Запуск Сидирования:** Выполните скрипт для обновления базы данных и пересчета хеша:
    ```bash
    cd backend
    npm run seed:foundation
    ```

### 🧬 Почему это важно?
При запуске скрипта:
- Рассчитывается **SHA-256 хеш** всего контента.
- Хеш записывается в `FoundationAuditLog`.
- Это позволяет системе доказать в будущем, что пользователь принял именно ТУ версию правил, которая была актуальна на момент его входа.

### 🧩 Маркеры в системе
В базе данных эти объекты помечены:
- `Course.type = 'FOUNDATIONAL'`
- `Material.tags -> is_admission_block = true`

Если вы видите эти флаги в коде фронтенда — **блокируйте кнопку "Редактировать" (Edit)**. Весь контент должен быть Read-Only.

---
*С уважением, Канонический AI-ассистент MatrixGin.*
