# 04-charging-and-spending.md
## Charging & Spending — Domain Rulebook (MC / GMC)

---

## 0. Назначение документа

Документ описывает **как в системе происходит начисление и использование**:
- Matrix Coin (MC);
- Golden Matrix Coin (GMC).

Цель:
- убрать путаницу между «начислением», «использованием» и «признанием»;
- дать операционную инструкцию для запуска и поддержки.

Документ:
- **не является CANON**;
- может дополняться;
- не вводит экономических трактовок.

---

## 1. Базовые различия (ОБЯЗАТЕЛЬНО ПОНЯТЬ)

### 1.1. MC
- **начисляется**;
- **используется**;
- **списывается**;
- является краткоживущей операционной сущностью.

### 1.2. GMC
- **не начисляется**;
- **не списывается**;
- **признаётся**;
- является долгоживущей стратегической сущностью.

👉 Если это различие нарушено — система ломается.

---

## 2. Начисление MC (Charging)

### 2.1. Когда MC начисляются

MC начисляются **только**:
- за зафиксированное действие;
- при выполнении условий;
- через явное событие начисления.

MC **не начисляются**:
- «за период»;
- за статус;
- за лояльность;
- по решению AI.

---

### 2.2. Процесс начисления MC

1. Пользователь совершает действие
2. Действие проходит валидацию
3. Фиксируется событие `MCAccrued`
4. MC становятся доступными (Available)

Каждое начисление:
- имеет источник;
- имеет audit-запись;
- воспроизводимо.

---

## 3. Использование MC (Spending)

### 3.1. Что считается использованием

Использование MC — это:
- участие в аукционе;
- доступ к разрешённому действию;
- любой сценарий, где MC являются условием.

---

### 3.2. Резервирование MC

Перед использованием:
- MC могут быть переведены в **Reserved**;
- резерв — временный;
- резерв всегда фиксируется.

Если сценарий не завершён:
- резерв снимается;
- MC возвращаются в Available.

---

## 4. Списание MC (Consumption)

### 4.1. Когда MC считаются списанными

MC считаются списанными, если:
- сценарий завершён успешно;
- зафиксировано событие `MCConsumed`.

После списания:
- MC **не возвращаются**;
- MC **не пересчитываются**;
- MC остаются в audit как факт.

---

### 4.2. Где часто путаются (ВАЖНО)

❌ Ошибка: «MC списались при ставке»  
✅ Правильно: MC списываются **только после завершения сценария**

❌ Ошибка: «ставка = списание»  
✅ Правильно: ставка = **условие**, списание = **результат**

---

## 5. GMC и «списание» (ЧАСТАЯ ОШИБКА)

### 5.1. GMC НЕ списываются

GMC:
- не уменьшаются;
- не тратятся;
- не сгорают.

Любая формулировка «потратить GMC» — **ошибка**.

---

### 5.2. Что реально происходит с GMC

С GMC происходит:
- признание;
- блокировка;
- активация;
- архивация.

Не происходит:
- расхода;
- обмена;
- конвертации.

---

## 6. Смешанные сценарии (MC → GMC)

Типовой сценарий:

1. Пользователь получает MC
2. Пользователь участвует в аукционе (MC используются)
3. Аукцион закрывается
4. MC победителя списываются
5. GMC признаётся

👉 **MC и GMC никогда не существуют в одном действии как «обмен»**.

---

## 7. Ошибки и сбои

### 7.1. Ошибка до завершения сценария

- MC не списываются;
- резерв снимается;
- фиксируется ошибка.

### 7.2. Ошибка после завершения

- состояние не пересчитывается автоматически;
- требуется ручной разбор;
- audit остаётся источником истины.

---

## 8. Запреты

Запрещено:
- списывать MC без события;
- начислять MC задним числом;
- «компенсировать» MC вручную;
- уменьшать GMC;
- конвертировать GMC в MC.

---

## 9. Краткое резюме

- MC: начисление → использование → списание.
- GMC: признание → (блокировка) → активация → архив.
- Списание ≠ ставка.
- Признание ≠ начисление.
