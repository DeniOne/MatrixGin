# AUDIT REMEDIATION — Module 02: Employee Management

> **Статус аудита (2026-01-19):** ❌ REJECTED  
> **Аудитор:** Antigravity (TechLead)

## ❌ Критические проблемы (BLOCKERS)

### 1. Frontend: Полное отсутствие интерфейса
- **Sidebar**: Пункт "Сотрудники" ведет на `/employees`, но этот маршрут не зарегистрирован в `App.tsx`.
- **Компоненты**: Сама страница списка сотрудников или карточки профиля не реализованы (найдены только файлы в OFS, которые не являются полноценным управлением сотрудниками).
- **Цель**: Реализовать UI, позволяющий видеть людей в системе, как того требует раздел 1 `MODULE-SPEC.md`.

### 2. Этика: Нарушение архитектурных запретов
- **Файл**: `frontend/src/pages/mgmt/MgmtPeoplePage.tsx`
- **Проблема**: В заглушке указаны "Процесс оценки 360°" и "Матрица грейдов".
- **Нарушение**: Согласно разделу 6 `MODULE-SPEC.md` и разделу `DEFERRED` в чеклисте, любые формы оценки, рейтингов и "performance review" **строго запрещены** в рамках этого модуля.
- **Действие**: Полностью переписать или удалить заглушку, убрав упоминания карательных или оценивающих инструментов.

## ⚠️ Несоответствие документации
- **Файл**: `IMPLEMENTATION-CHECKLIST.md`
- **Проблема**: В документе ошибочно проставлен статус `✅ PASS`, что скрывает реальное состояние модуля.
- **Действие**: Обновить статус на `❌ REJECTED` до устранения проблем на фронтенде.

## 🛠️ План доработки

1. [ ] Зарегистрировать маршрут `/employees` в `App.tsx`.
2. [ ] Создать `EmployeesPage.tsx` (базовый список на основе `ofsApi.getEmployees` или создать `employeeApi`).
3. [ ] Реализовать `EmployeeCard` для просмотра деталей сотрудника с учетом ACL (не запрашивать зарплату для не-HR).
4. [ ] Очистить контент `MgmtPeoplePage.tsx` от анти-каноничных терминов (360°, грейды).
5. [ ] Синхронизировать чеклисты с реальностью.
